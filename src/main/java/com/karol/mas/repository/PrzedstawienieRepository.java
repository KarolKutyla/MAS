package com.karol.mas.repository;

import com.karol.mas.entity.Przedstawienie;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrzedstawienieRepository extends JpaRepository<Przedstawienie, Long> {
}
