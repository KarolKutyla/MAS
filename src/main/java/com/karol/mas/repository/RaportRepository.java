package com.karol.mas.repository;

import com.karol.mas.entity.Raport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RaportRepository extends JpaRepository<Raport, Long> {
}
