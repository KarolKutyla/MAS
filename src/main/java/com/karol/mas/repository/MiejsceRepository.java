package com.karol.mas.repository;

import com.karol.mas.entity.Miejsce;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MiejsceRepository extends JpaRepository<Miejsce, Long> {
}
