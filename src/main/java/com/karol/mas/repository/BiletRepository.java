package com.karol.mas.repository;

import com.karol.mas.entity.Bilet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BiletRepository extends JpaRepository<Bilet, Long> {
}
